# Architecture

`modules/desktop` contains desktop environment behavior.

`modules/programs` contains application configuration.

`modules/services` contains system and user services.

`modules/themes` contains visual configuration.

`upstream/` records pinned source data and porting progress.
