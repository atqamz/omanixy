# Tasks

Each task records one bounded unit of work.

Use statuses `proposed`, `ready`, `in-progress`, `blocked`, `review`, and
`done`.

Move completed tasks into `tasks/done/` after the task is committed.
